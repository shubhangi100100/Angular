import { PowerBoostPipe } from './power-boost.pipe';

describe('PowerBoostPipe', () => {
  it('create an instance', () => {
    const pipe = new PowerBoostPipe();
    expect(pipe).toBeTruthy();
  });
});
